# Dépendances Composer (réinstallables — jamais commitées dans un package)
/vendor/
composer.lock

# Dépendances Node (outil de build des assets JS — réinstallables via `npm install`)
/node_modules/
npm-debug.log*

# NB : resources/js/dist/ EST commité volontairement (c'est ce que le package sert).

# IDE / outils
.idea/
.vscode/
.claude/

# macOS
.DS_Store
